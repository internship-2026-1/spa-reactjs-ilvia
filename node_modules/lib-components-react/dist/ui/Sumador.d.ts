import "../styles.css";
type SumadorProps = {
    value: number;
    onChange: (value: number) => void;
    min?: number;
    max?: number;
};
export default function Sumador({ value, onChange, min, max, }: SumadorProps): import("react/jsx-runtime").JSX.Element;
export {};
