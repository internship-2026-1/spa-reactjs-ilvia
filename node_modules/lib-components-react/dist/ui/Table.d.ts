import React from 'react';
export type Column<T> = {
    key: keyof T | string;
    header: string;
    render?: (row: T) => React.ReactNode;
    className?: string;
};
type TableProps<T> = {
    data: T[];
    columns: Column<T>[];
    keyField?: keyof T;
    emptyMessage?: string;
    itemsPerPage?: number;
};
export declare function Table<T>({ data, columns, keyField, emptyMessage, itemsPerPage }: TableProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
